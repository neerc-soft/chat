#summary XMPP
#labels Phase-Design

= Links =

[http://www.igniterealtime.org/downloadServlet?filename=openfire/openfire_3_6_4.tar.gz Openfire 3.6.4 Linux]
[http://www.igniterealtime.org/downloadServlet?filename=openfire/openfire_3_6_4.zip Openfire 3.6.4 Win]
= Server installation =
 # Start server
{{{
./bin/openfire start

(win: ./bin/openfire.exe)
}}}
 # Open http://localhost:9090 and follow installation instructions (use *Embedded Database* and set server name to *localhost*)
 # Restart server
{{{
./bin/openfire stop
./bin/openfire start
}}}
 # Open http://localhost:9090 and login as admin
 # Install "User Import Export" plugin
 # Import users from openfire-users.xml
 # Create persistent room with following settings:
  * Room Name: neerc
  * Broadcast Presence for: Moderator, Participant, Visitor
  * Show Real JIDs of Occupants to: Moderator
  * Room Options: Log Room Conversations

= XEPs =

Full list: http://xmpp.org/extensions/

Useful for us:
 # [http://xmpp.org/extensions/xep-0045.html Multi-User Chat]
 # [http://xmpp.org/extensions/xep-0199.html XMPP Ping]
 # [http://xmpp.org/extensions/xep-0013.html Flexible Offline Message Retrieval] (???)
 # [http://xmpp.org/extensions/xep-0004.html Data Forms] - task management
 # [http://xmpp.org/extensions/xep-0184.html Message Receipts] (for private messages, not needed for MUC)

= Regression =

Sorted by importance:

|| *Feature* || *Status* ||
|| TaskMessage || implemented ||
|| Power users || implemented ||
|| ServerMessage (join, left) || implemented ||
|| UserMessage (public) || implemented ||
|| TimerMessage || ||
|| WelcomeMessage || don't needed - use history ||
|| UserMessage (private) || ||
|| PingMessage || don't needed? ||
|| UserListUpdateMessage || don't needed ||
|| LoginMessage || don't needed ||

= Implementation Details =

Each MUC Message stored on server, so history can be easily retrieved by clients.